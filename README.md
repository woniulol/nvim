# nvim
Neovim config

# Usage
```
cd ~/.config
git clone https://github.com/woniulol/nvim.git

nvim
```

## For Linux:
```
sudo apt update
sudo apt-get update
sudo apt install build-essential cmake -y
sudo apt-get install python3.12.venv
```

